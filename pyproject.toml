[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "plotting"
version = "0.1.0"
description = "Collection of plotting routines."
requires-python = ">=3.9"

[tool.setuptools]
packages = ["plotting"]
